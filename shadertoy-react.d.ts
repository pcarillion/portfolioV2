declare module "shadertoy-react" {
  // You can declare the types for the exported components or functions here.
  // For now, you can declare it as `any` to avoid TypeScript errors.
  const ShadertoyReact: any;
  export default ShadertoyReact;
}
